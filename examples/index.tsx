import React, { Component } from 'react';
import DialButton from '../lib';

class index extends Component {
    render() {
        return (
            <div>
                <DialButton>

                </DialButton>
            </div>
        );
    }
}

export default index;